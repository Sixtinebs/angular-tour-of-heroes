// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyBircRdDbov3omKKD7_puckwMQUeupeAFg',
    authDomain: "violin-pro.firebaseapp.com",
    databaseURL: "https://violin-pro.firebaseio.com",
    projectId: "violin-pro",
    storageBucket: "violin-pro.appspot.com",
    messagingSenderId: "604262829895",
    appId: "1:604262829895:web:3390cc327d408b5b254662"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
